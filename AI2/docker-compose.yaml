version: '1.0'
name: pythotch_server

services:
  pythotch:
    restart: unless-stopped
    image: pytorch:0.0.1
    volumes:
      - C:\Users\User\AI2\data:/usr/app/src
    command: python3 /usr/app/src/convolution.py